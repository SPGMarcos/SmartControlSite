VITE_API_URL=https://smartcontrolsite.onrender.com/api

# Optional for local development when using relative /api calls through Vite.
# Example: set this to your local Django origin in your private frontend/.env file.
# VITE_DEV_API_PROXY_TARGET=http://<backend-host>:<backend-port>
